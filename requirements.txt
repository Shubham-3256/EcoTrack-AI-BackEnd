# ----- Flask Core -----
Flask==2.3.3
Flask-SQLAlchemy==3.0.3
Flask-Cors==4.0.0
Werkzeug>=2.3

# ----- Authentication -----
PyJWT==2.8.0

# ----- Password hashing (legacy + new) -----
bcrypt>=4.1.2      # for verifying old bcrypt hashes
# werkzeug.security handles PBKDF2 hashing now

# ----- Data & ML -----
pandas==2.1.1
scikit-learn==1.3.2
joblib==1.3.2

# ----- Environment variable loader -----
python-dotenv==1.0.0

# ----- Database driver (if you ever switch to PostgreSQL) -----
# psycopg2-binary>=2.9.7   # OPTIONAL: only needed if using PostgreSQL

# ----- Misc -----
# Add others here if needed (requests, etc.)
# For production server
gunicorn>=21.2.0